import React, { Fragment } from "react";
import styled from "styled-components";
import Header from "./Header";

const Home = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default Home;
